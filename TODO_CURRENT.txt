NIKLAR — TODO (current execution queue)
Last updated: 2026-08-09 (DONE — Batch 1 patch applied per RESPONSE_SEQ
10's "CHANGES REQUIRED before Batch 2"; niklar-stocks 221020b;
HANDOFF_SEQ 8. Batch 2 remains HOLD pending re-review.)

HOLD — Batch 2 pending Batch 1 patch re-review (this update)
---------------------------------------------------------------------------
  - Real, valid review findings were missed for one cycle: RESPONSE_SEQ
    10 ("CHANGES REQUIRED before Batch 2") was superseded by
    RESPONSE_SEQ 11 in the Drive sync doc's compact LATEST_RESPONSE
    block before it was ever read -- checking only the current block
    missed it. Recovered by searching Drive for ChatGPT's own
    response-archive doc rather than trusting RESPONSE_SEQ 11's brief
    cross-reference alone. Process correction recorded in decision #32.
  - Patched both findings against commit 53d2b3f (niklar-stocks
    221020b): (1) normalization_exception_count now derived from
    actual REVIEW_REASON_NORMALIZATION_EXCEPTION review_queue entries,
    not evidence-status totals; (2) added bounded compact
    pending_review_items/pending_llm_review_items so the snapshot
    identifies exact ticker/reason, not just counts. Also fixed the
    accompanying non-blocking observation: compute_evidence_status()
    now returns all_unresolved_fields alongside the materially-
    blocking subset. 8 new tests (27->35). unittest 130/130, mypy
    0/14, PWA smoke unaffected, real GitHub Actions run 31277370000
    confirmed success.
  - Full patched diff published in niklar-handoff
    review-packages/221020b-batch1-patch.md for independent
    re-review, per RESPONSE_SEQ 10's explicit "hand back for compact
    re-review" requirement. Batch 2 (non-blocking normalization
    exception path + owner-gated LLM policy + deterministic LLM
    dependency audit tooling) remains HOLD until that re-review passes
    -- not started, per explicit "do not start Batch 2 in parallel."

ACKNOWLEDGED, DEFERRED — Section 19: PWA<->Slack notification controls (prior update)
---------------------------------------------------------------------------
  - Drive sync doc RESPONSE_SEQ 9: owner approved an addition to the
    pre-live architecture doc (Section 19) -- manual single-ticker/
    consolidated-brief Slack preview+send controls, preview-before-
    send, stale/caveat gate, deterministic notification_id dedup,
    delivery-status visibility, and mandatory convergence between
    automatic and manual sends on one notification service/renderer/
    idempotency/retry/Change-Ledger-recording path. No Slack
    credentials ever exposed to the PWA.
  - Explicit instruction: "Do not derail Batch 1. Continue the
    already-authorized staged plan. This Slack/PWA delta belongs when
    the mobile delivery/PWA interaction + notification service layers
    are implemented" -- read the full text from the primary Drive doc,
    recorded as decision #31, no implementation now. Applies to Batch 4
    (and touches Batch 3's discovery/tactical consumer-sync stages).
  - RESPONSE_SEQ 9 crossed in transit with HANDOFF_SEQ 6 (Batch 1's
    publish) -- doesn't address Batch 1 review yet. No niklar-stocks
    mutation this cycle. Next: check for Batch 1 review feedback, then
    begin Batch 2 (non-blocking normalization exception path +
    owner-gated LLM policy + deterministic LLM dependency audit
    tooling) regardless, since overall IMPLEMENTATION AUTHORIZATION
    remains RELEASED and nothing holds Batch 2 specifically.

DONE — pre-live architecture Batch 1: contracts/state layer (prior update)
---------------------------------------------------------------------------
  - Owner approved the full pre-live architecture (Drive
    NIKLAR_PRELIVE_ARCHITECTURE_DECISIONS_CURRENT, RESPONSE_SEQ 8,
    IMPLEMENTATION AUTHORIZATION: RELEASED) — 18 sections, 10 staged
    implementation batches. Per explicit instruction ("Do not attempt
    one giant refactor... begin Batch 1 only"), implemented ONLY
    Batch 1: ops/niklar_state_contracts.py (evidence status
    classification, INTRADAY_OBSERVATION contract, change ledger,
    review queue, LLM/manual-review request ledger, current state
    snapshot builder) -- pure, deterministic, no I/O/credentials.
    27 new tests. unittest 122/122, mypy 0/14, real GitHub Actions run
    31276045591 confirmed success.
  - Self-audited against the standing 4 criteria before publishing --
    no findings. Full diff published in niklar-handoff
    review-packages/53d2b3f-prelive-batch1-state-contracts.md for
    independent review (ChatGPT's connector still can't reach the
    private repo directly). Recorded as decisions #29 (owner approval
    of the full architecture) and #30 (this batch's implementation).
  - Batches 2-10 (normalization/LLM-gating, orchestrator wiring, PWA
    delivery contract, manual/OCR intraday input, ScreenCaptureKit
    feasibility, GitHub Actions production scheduling, backup/
    recovery, full pre-live simulation) are NOT started. Next sync
    will check for review feedback on Batch 1 before starting Batch 2.

PRIOR — AWAITING CHATGPT AUDIT — architecture decision package (superseded by owner approval above)
---------------------------------------------------------------------------

AWAITING CHATGPT AUDIT — decision package for architecture choice (this update)
---------------------------------------------------------------------------
  - User established a new standing rule (Drive sync doc RESPONSE_SEQ
    6): every material Claude recommendation requiring user judgment
    must be independently audited by ChatGPT first. Published a
    compact decision-review package for the orchestrator/live-data
    architecture decision (4 options, recommendation, tradeoffs,
    cost/complexity, credentials/security, mobile practicality,
    integrations, reversibility, canon boundaries, unknowns) — full
    content in CURRENT_HANDOFF.txt (HANDOFF_SEQ 5), not repeated here.
  - No implementation, no user-facing choice requested yet. Recorded
    as decision #28. Waiting on ChatGPT's audit before this becomes
    decision-ready for the user.

RELEASED — independent-review hold on ee594cc (prior update)
---------------------------------------------------------------------------
  - ChatGPT's Drive sync doc RESPONSE_SEQ 3: independent review of the
    trimmed review package (published at HANDOFF_SEQ 3) completed --
    "PASS WITH NON-BLOCKING SIMPLIFICATION." Failure-envelope review:
    PASS. Security/privacy review: PASS. Dependency/abstraction review:
    PASS. Minimum sufficient code: PASS with two non-blocking stylistic
    notes (both explicitly deferred by ChatGPT to a future nearby
    touch, not actioned now: shorten duplicated commentary in the new
    except block/README/tests; adjust the error message's "while
    reading the response" wording since the catch scope is slightly
    broader). CANON/GATE STATUS: PASS.
  - "The independent-review hold on ee594cc is released. Claude may
    resume material implementation within the already-authorized
    Niklar scope... No QA-infrastructure expansion is authorized
    merely because this review completed."
  - On "cont": checked the queue for the next safe, unblocked,
    already-authorized material increment. None found -- the only
    remaining AWAITING USER DECISION item (orchestrator stage-handler /
    live-data-source architecture, below) is separate and was never
    gated on this review. No niklar-stocks mutation this cycle.
    Recorded as decision #27.

PRIOR HELD — material implementation expansion (now released, see above)
---------------------------------------------------------------------------
  - Unchanged from RESPONSE_SEQ 1: "CONT AUTHORIZATION: PASS only for
    targeted review-evidence preparation, handoff/archive mechanics,
    and trivial non-material coordination cleanup. Material
    implementation expansion remains held until the independent review
    can inspect actual relevant code/diff." No niklar-stocks work
    beyond this hold until that review clears.

DONE — trimmed review package per "keep it minimal" (this update)
---------------------------------------------------------------------------
  - ChatGPT's RESPONSE_SEQ 2 (crossed in transit with HANDOFF_SEQ 2 --
    written before that checkpoint was visible, still references
    HANDOFF_SEQ 1) refined the review-package request: "Keep it
    minimal... Exclude... broad source snapshots." The HANDOFF_SEQ 2
    package had included the full 207-line gemini_client.py as context
    -- trimmed to just the diagnose() function (101 lines, the one
    function the fix touches), so all four except clauses (incl. the
    new one) stay visible together without the unrelated module-level
    content (docstring, imports, constants, other class definitions).
    Re-scanned for secrets/canon after trimming -- clean.
  - Caught and fixed a real gap from the HANDOFF_SEQ 2 cycle itself: a
    header edit to archive/handoffs/LEGACY_PRE_SEQUENCE_SELF_AUDIT_EE594CC.txt
    had been made in the working tree but never actually staged after
    the git mv that renamed it -- found via git diff against HEAD
    before this commit, not assumed already-committed. Now included.
  - Archived the addressed HANDOFF_SEQ 2 to archive/handoffs/HANDOFF_SEQ_2.txt
    before publishing HANDOFF_SEQ 3. Recorded as decision #26. No
    niklar-stocks mutation this cycle.

DONE — archive restructured + review package published (prior update)
---------------------------------------------------------------------------

HELD — material implementation expansion (as of prior update, RESPONSE_SEQ 1)
---------------------------------------------------------------------------
  - ChatGPT's Drive sync doc RESPONSE_SEQ 1 (2026-08-09): "CONT
    AUTHORIZATION: PASS only for the handoff/archive migration and
    other trivial non-material coordination cleanup. Material
    implementation expansion remains held until the independent review
    of ee594cc can inspect actual code/diff." No niklar-stocks work
    beyond this hold until that review clears.

DONE — review package published for ee594cc (this update)
---------------------------------------------------------------------------
  - ChatGPT's GitHub connector returned 404 attempting to direct-fetch
    the private niklar-stocks repo for independent review of commit
    ee594cc (the gemini_client.py self-audit fix) — the compact review
    handoff published at HANDOFF_SEQ 1 wasn't independently
    verifiable without the actual code. Published
    review-packages/ee594cc-gemini-client-self-audit.md: full commit
    message, exact diff, full new test file (14 tests), full post-fix
    gemini_client.py, and a checklist mapped to the 6 review criteria.
    Scanned for secrets/canon before publishing (clean).

DONE — archive restructured to match ChatGPT's exact spec (this update)
---------------------------------------------------------------------------
  - ChatGPT's response crossed in transit with Claude's own independent
    HANDOFF_SEQ 1 refactor (same cycle) -- reconciled to match exactly:
    moved HANDOFF_HISTORY_ARCHIVE.txt from repo root to
    archive/handoffs/LEGACY_PRE_SEQUENCE_SELF_AUDIT_EE594CC.txt (the
    exact stable ID ChatGPT's own RESPONSE_TO_HANDOFF field used);
    archived the addressed HANDOFF_SEQ 1 checkpoint to
    archive/handoffs/HANDOFF_SEQ_1.txt before publishing HANDOFF_SEQ 2;
    added the missing CANON/GATE STATUS field to the compact checkpoint
    format. README.md updated to match.
  - Recorded as decision #25. No niklar-stocks mutation this cycle
    (mechanics/coordination only, respecting the explicit hold above).

DONE — handoff mechanics: compact latest-pointer form (prior update)
---------------------------------------------------------------------------
  - Drive sync doc's "HANDOFF DISCOVERY + COMPACT DELTA OPTIMIZATION"
    entry (2026-08-09) froze a new operational rule: CURRENT_HANDOFF.txt
    converts from an accumulating narrative to a compact checkpoint
    (STATUS/PROJECT/HANDOFF_SEQ/CHECKPOINT_ID/IMPLEMENTATION_COMMIT/
    HANDOFF_COMMIT/GOAL/MATERIAL_DELTA/VALIDATION/SELF_AUDIT/
    PROTECTED_BOUNDARY_STATUS/BLOCKERS/REVIEW_REQUEST/NEXT_ACTION), with
    a monotonic HANDOFF_SEQ per material checkpoint, and direct fetch of
    that exact file becomes the primary sync/status discovery path
    (repository discovery/search is not the primary availability check).
  - Rewrote CURRENT_HANDOFF.txt in the compact form, HANDOFF_SEQ 1.
    Preserved the full 650-line prior narrative verbatim in a new file,
    HANDOFF_HISTORY_ARCHIVE.txt — nothing deleted, only relocated.
    Updated README.md: new "Handoff discovery" section explaining the
    convention, "Structure" section updated for both files.
  - Mechanics-only: no Niklar product/trading logic touched, no
    niklar-stocks mutation this cycle. Recorded as decision #24.

DONE — self-audit of scripts/ai_qa/, first reference case (prior update)
---------------------------------------------------------------------------
  - ChatGPT's Drive sync doc (2026-08-09 01:40 +08:00 entry) named
    scripts/ai_qa/ as the first reference case for its newly frozen
    independent engineering review pattern: self-audit against Minimum
    Sufficient Code / unnecessary abstraction / duplicate logic /
    missing failure-envelope coverage / security-privacy leakage /
    architecture-canon drift, then a compact review handoff for
    ChatGPT to independently inspect the actual diff.
  - Read all 5 core modules end to end. Found one real defect in
    gemini_client.py: the try/except around the Gemini HTTP call
    handled HTTPError/URLError/TimeoutError (connection-setup
    failures) but NOT a connection that resets, or an incomplete body,
    mid-response (ConnectionResetError; http.client.IncompleteRead,
    which isn't even an OSError subclass). run.py's main() only
    catches GeminiError, so this would have crashed the whole worker
    and lost the deterministic FAIL report -- contradicting the
    module's own "never an unhandled traceback" claim.
  - Fixed with `except (OSError, http.client.HTTPException)`, same
    graceful-degrade-to-GeminiError path as every other failure mode.
    Also closed gemini_client.py's prior zero-test-coverage gap in the
    same pass: added tests/test_ai_qa_gemini_client.py (14 tests,
    fully mocked, no real network call) covering the happy path, every
    network-failure mode (incl. 2 regression tests for this exact
    finding), and every malformed-response shape the code already
    handled. Reviewed the other 4 modules against the same 6 criteria
    -- no further findings.
  - Full validation: unittest 95/95 (81->95), mypy 0/13, PWA smoke
    unaffected. Real GitHub Actions run polled directly and confirmed
    conclusion: "success" (not just local success). Gate PASS/PASS.
    niklar-stocks main: ee594cc.
  - Recorded as decisions #22 (adopting the pattern) and #23 (the
    finding + fix). Review handoff published here for ChatGPT's
    independent inspection per the pattern's own requirement.

DONE — Gemini escalation enabled and verified end-to-end (prior update)
------------------------------------------------------------------------
  - User confirmed directly ("Yes, added it") that GEMINI_API_KEY is
    present as a niklar-stocks repository secret. Flipped the second
    gate: AI_QA_ENABLE_GEMINI hardcoded to "1" in
    .github/workflows/ai-qa.yml (commit 2be671a). Polled the real
    GitHub Actions run for this enable-only commit -- confirmed
    conclusion: "success" before proceeding further.
  - Per ChatGPT's "NEXT CHECKPOINT" instruction: introduced one
    deliberate, temporary, controlled failure (a mechanical, canon-free
    mypy type error in scripts/ai_qa/config.py, commit 66caa0b) to
    force a real CI failure and confirm Gemini is genuinely invoked.
    Polled the real run's job log directly and confirmed a genuine
    round-trip against the live endpoint: "Gemini diagnosis:
    MECHANICAL_FIX (HIGH confidence)" -- a real structured
    classification, not just clean error handling (which is all the
    earlier invalid-key check had proven). No canon content, secrets,
    or repository material appeared in the run log.
  - Reverted the deliberate failure immediately (commit a937f87,
    confirmed byte-for-byte identical to 2be671a's config.py). Local
    re-check: unittest 81/81, mypy 0 issues. Polled the real run for
    the revert commit -- confirmed conclusion: "success", real CI green
    again.
  - Recorded as decision #21. This closes the item below (was AWAITING
    USER DECISION) and the last open step from ChatGPT's "NEXT
    CHECKPOINT" instruction.

DONE — verified and fixed the real CI run (prior update)
------------------------------------------------------------
  - Checked the actual GitHub Actions run for the new ai-qa.yml
    workflow instead of trusting local testing/the prior report's
    "GitHub Actions ready: YES" claim -- it was wrong. Real run failed
    (ImportError: relative import). Fixed (workflow + 5 docs needed
    `python3 -m scripts.ai_qa.run`, not the direct file-path form),
    re-validated locally, pushed, polled the real API again -- now
    genuinely conclusion: "success". Gate PASS/PASS.

RESOLVED (was AWAITING USER DECISION)
------------------------------
  - Turning on Gemini escalation in niklar-stocks scripts/ai_qa/ for
    real: add the GEMINI_API_KEY GitHub Actions secret and set
    AI_QA_ENABLE_GEMINI=1 (secret or repo variable) when ready. The
    worker is fully implemented, tested (incl. live against the real
    Gemini endpoint with an invalid key), and ships inert — this repo's
    first outbound network call/credential, deliberately not
    self-authorized. See niklar-stocks scripts/ai_qa/README.md and
    AGENTS.md "Held decisions."
  - UPDATE 2026-08-09: ChatGPT reviewed and conditionally authorized
    proceeding — "once the repository secret GEMINI_API_KEY is
    present... enable it only after confirming the secret is available
    in the repository Actions environment." No tool in this session can
    list/confirm GitHub Actions secret presence (write-only by design).
    Awaiting the user to confirm directly whether GEMINI_API_KEY has
    been added yet — not proceeding on a guess. Once confirmed: set
    AI_QA_ENABLE_GEMINI=1, trigger one controlled CI failure-path
    invocation to confirm real end-to-end escalation, then checkpoint
    per ChatGPT's "NEXT CHECKPOINT" instruction.
  - RESOLVED 2026-08-09: see "DONE — Gemini escalation enabled and
    verified end-to-end" above. Both gates are live, real end-to-end
    verification complete. AGENTS.md's "Held decisions" section moved
    this to a new "Resolved decisions" section.

DONE — AI QA worker port (this update)
---------------------------------------------
  - Ported the proven Gemini QA worker pattern from
    joiedevivre02/journey-passport (found on an unmerged branch,
    commit bf4fb61 -- read in full before designing anything) into
    niklar-stocks/scripts/ai_qa/. Deterministic-first, fail-fast,
    Gemini-on-failure-only, diagnosis-only (never edits code, never
    decides pass/fail). .github/workflows/tests.yml replaced with
    ai-qa.yml (confirmed safe via GitHub API -- no branch protection).
  - Found and mitigated a real risk the source pattern didn't have:
    this repo's own test failures can leak canonical trading logic
    verbatim (verified against the real render_preopen() case, not
    just synthetic). New module failure_extract.py strips test-step
    output down to test id/exception class/traceback location only --
    zero assertion-message content, ever.
  - Gemini escalation ships OFF by default (see AWAITING USER DECISION
    above) -- structural, not incidental (gated on an explicit flag,
    not just the key's presence).
  - Full validation: unittest 81/81 (17 new), mypy 0/13 files, PWA
    smoke unaffected, worker tested live 3 ways incl. against the real
    Gemini endpoint. Gate PASS/PASS.

DONE — cross-repo reference fix (prior update)
--------------------------------------------------
  - After the round-2 routing cleanup renamed ENGINEERING_AUTHORITY_
    PROMPT.txt, grepped niklar-stocks for the old filename and found
    2 genuinely stale references (AGENTS.md x2,
    docs/app/NIKLAR_APP_SYSTEM_MAP.md). Fixed both with a short
    "archived, not live" clarifying note. Full validation re-run
    (unittest 64/64, smoke test PASS, mypy 0/6). Gate PASS/PASS.
  - Also recorded a new user-facing convention (not a protocol
    change): Claude tells the user "sync to chatgpt" whenever
    publishing a checkpoint meant for ChatGPT to review. See
    CLAUDE_OVERNIGHT_AUTONOMOUS_PROMPT.txt.

DONE — routing cleanup, round 2 (prior update)
--------------------------------------------------
  - A more specific follow-up "REQUIRED CLAUDE GITHUB-SIDE CLEANUP"
    instruction (Drive sync doc) required: renaming
    ENGINEERING_AUTHORITY_PROMPT.txt to make its archival status
    unmissable from the filename (done: git mv to
    ARCHIVED_ENGINEERING_AUTHORITY_SNAPSHOT_2026-08-08.txt); moving
    README's communication rule before every other workflow section
    (done); a 5th acceptance-test question (done); updating remaining
    filename references (done); auditing CLAUDE_STATUS.txt and
    CLAUDE_OVERNIGHT_AUTONOMOUS_PROMPT.txt (done — clean, no edit
    needed). See decision #17. ROUTING ACCEPTANCE TEST: PASS (5/5,
    self-verified against README.md before declaring).

DONE — routing cleanup, round 1 (prior update)
----------------------------------------------------
  - ChatGPT's "ROUTING CLEANUP INSTRUCTION" (Drive sync doc) asked for
    this repo to stop implying ChatGPT responses can be obtained from
    GitHub, specifically calling out ENGINEERING_AUTHORITY_PROMPT.txt.
    Fixed: that file (now renamed, see above) headed "ARCHIVED
    SNAPSHOT — NOT LIVE"; README "Communication channel" section
    rewritten. See decision #16.

RESOLVED — engineering-authority instruction located (Drive, not GitHub)
--------------------------------------------------------------------------
  - Prior blocker: joiedevivre02/niklar-operating-engineering-system
    contained no ChatGPT-authored engineering-authority instruction
    for Niklar stocks (confirmed: it's a different, separately
    governed Niklar product with zero stocks-domain references).
  - User corrected the search location: ChatGPT cannot write directly
    to GitHub, so its instructions land in Google Drive instead. Found
    it: a Drive document that is a live ChatGPT -> Claude outbound
    sync channel, dated/modified 2026-08-08 (the newest matching
    content in Drive).
  - That document confirms the GitHub repo's role exactly as already
    concluded: READ-ONLY REFERENCE MATERIAL for Niklar Stocks, subject
    to a scope firewall (project-agnostic engineering only; its
    product/domain material must never enter niklar-stocks). Nothing
    about the prior investigation was wrong — the missing piece was
    only the *location* of the ChatGPT-authored instruction itself.
  - ENGINEERING_AUTHORITY_PROMPT.txt created in this repo, reproducing
    the Drive document's content near-verbatim (sanitized — no Drive
    IDs/links/emails).
  - Scope-firewall compliance audit run against niklar-stocks (working
    tree + full git history, all branches): zero hits for any
    property-transfer/guided-journey domain vocabulary (journey
    control number, verified save, workspace saving, property
    eligibility, case record recovery, marketplace orchestration,
    founder constitution, knowledge warehouse, etc.). The reconnaissance
    clone of niklar-operating-engineering-system lives outside the
    niklar-stocks working tree and is not referenced, committed,
    submoduled, or depended on anywhere in niklar-stocks.
    ENGINEERING AUTHORITY CHECK: PASS
    ENGINEERING SCOPE FIREWALL: PASS
    PROPERTY-TRANSFER MATERIAL IN NIKLAR-STOCKS: NO
  - niklar-stocks implementation may now resume, per the user's own
    "do not continue until loaded" instruction — the authority is
    loaded. See NEXT PHASE below for what it actually authorizes next.

NEXT PHASE — COMPLETE (niklar-stocks commit ab74109)
------------------------------------------------------------------------
  All 5 architecture artifacts prepared and the required provisional-
  implementation audit run against existing ops/mobile_contract.py/pwa/
  code (see CURRENT_HANDOFF.txt for the outcome summary; full table in
  niklar-stocks docs/app/NIKLAR_APP_CONTRACT.md). Zero REFACTOR
  REQUIRED, zero REMOVE-DO NOT RELY ON. Two interface gaps recorded
  (real-vs-simulated position marker; live data source) — both already
  known, not new. Drive canon, analytical rules, scoring, tactical
  semantics, invalidation logic, and market workflows were untouched.
  Test suite re-verified 55/55 passing. This checkpoint is the
  "next material checkpoint" ChatGPT asked to be left here for review.
  Per the checkpoint instructions: continue only within already-
  authorized safe scope until reviewed — no new feature lane, live
  data, or deployment beyond what NIKLAR_V0_1_SCOPE.md already states.

AWAITING USER DECISION
------------------------
  - Architecture/stack for the orchestrator's stage-handler
    implementations (authority_load, research_memory_gate,
    evidence_acquisition, decision_object_freeze,
    canonical_serialization, publication_validation,
    research_database_append, research_indexes_update,
    master_database_update, opportunity_matrix_rebuild,
    discovery_consumers_sync, tactical_consumers_sync,
    postflight_verification). These require deciding how/whether this
    environment integrates with Drive/Sheets/Slack and any market-data
    source — a material architectural + credentials decision, held for
    the user rather than guessed. Unchanged by this update.
  - Same underlying decision blocks a live data source for the PWA
    (currently reads a static synthetic sample-data.json only).

DEFERRED (superseded by NEXT PHASE above — not dropped, just not next)
------------------------------------------------------------------------
  - Further PWA views (Watchlist alerting, Daily Brief, Position/
    journal) — still also blocked on data/semantics this environment
    doesn't have, independent of the new STOP GATE.
  - mypy CI validation already added (9549f65) — 0 issues, ops/ only.
  - General ops/ technical-debt/reliability passes remain fair game if
    the architecture-artifact lane above stalls on something
    genuinely blocked, but are not the priority right now.

BLOCKED
----------
  - (none)

VALIDATION REQUIRED
----------------------
  - (none currently — see CURRENT_HANDOFF.txt for what was already run
    and passed on prior increments)

COMPLETED, AWAITING CANONICAL SYNCHRONIZATION
-------------------------------------------------
  - ops/ integration (cac4df1), mobile contract + PWA slice (56dd1d2),
    Opportunity Landscape (aae6fea), cross-section navigation
    (3a66e90), Watchlist roster (95cb051), mypy CI (9549f65) —
    private-repo-only code artifacts; not candidates for Drive canon
    promotion (code doesn't belong in the Drive canon layer), but
    flagged per the recovery rule as not yet reconciled anywhere else.
    No action needed unless the user wants any referenced from Drive.
