NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: PATCH_COMPLETE_AWAITING_REREVIEW (Batch 2) + RESOLVED (BATCH3-STAGE-MAPPING-01) + HARD_GATE (Batch 3 full scope, unchanged)
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 13
CHECKPOINT_ID: niklar-handoff-2026-08-09-seq13-batch2-patch2-batch3-mapping-resolved
IMPLEMENTATION_COMMIT: niklar-stocks main @ 02b6f88
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Respond to Drive sync doc RESPONSE_SEQ 17 (responding to
HANDOFF_SEQ 12) — patch Batch 2's three independent-review findings,
and record ChatGPT's resolution of delegated decision
BATCH3-STAGE-MAPPING-01.

MATERIAL_DELTA: Patched all three Batch 2 findings, each verified
against the actual code before fixing (not assumed correct on
ChatGPT's word alone — all three were real):
(A) `attempt_normalization()`'s review-entry `detail` was
`f"{field}: {exc}"`, which could leak raw/sensitive normalizer-
exception text into a review entry `build_current_state_snapshot()`
surfaces downstream. Fixed to `f"{field}: {type(exc).__name__}"` —
field name + exception type name only, never message/args. New
regression test embeds a fake secret in a normalizer's exception
message and proves it never reaches the review entry.
(B) `decide_llm_escalation()` returned
`QUARANTINED_PENDING_OWNER_AUTHORIZATION` when free LLM was
owner-authorized but the free provider was merely unavailable —
factually wrong, since authorization already exists. Added a distinct
`LLM_ESCALATION_QUARANTINED_PROVIDER_UNAVAILABLE` state, checked after
the paid-authorized gate (so an owner-approved paid path is never
masked by the free provider being down) and before the generic
no-authorization-at-all default. 3 new gate-ordering tests matching
ChatGPT's exact requested cases.
(C) `run_llm_dependency_audit()`'s single corpus-wide normalizer was
insufficient for a real heterogeneous disclosure/research corpus. Per
ChatGPT's explicit "do not over-engineer... Minimum Sufficient Code"
instruction, added a minimal per-item normalizer override
(`item.get("normalizer", normalizer)`) — not a registry/dispatch
framework. 2 new tests.

Patched only the same two files Batch 2 touched, no new file/
dependency. Full patched diff + point-by-point response to each
finding published in `review-packages/02b6f88-batch2-patch.md`.

Also recorded ChatGPT's resolution of `BATCH3-STAGE-MAPPING-01`:
**OPTION B** — do NOT implement standalone Batch 3 handlers or stubs,
explicitly rejecting Claude's own Option A recommendation. ChatGPT's
reasoning: a 2-of-13 partial wiring isn't end-to-end demonstrable
regardless (`run_pipeline()` requires all 13), the 2 "defensible"
mappings still assume upstream precomputed values this repo doesn't
own, and building unattached handlers now would be code written
because implementation time was available, not because the product
currently needs it — conflicts with Minimum Sufficient Code. Batch 3
stays fully untouched, no niklar-stocks mutation for it this cycle.

VALIDATION: unittest 153/153 (148 → 153, 5 new: 1 secret-leak
regression, 3 gate-ordering, 2 per-item-normalizer), mypy 0/15, PWA
smoke PASS. Real GitHub Actions run 31287431651 (commit 02b6f88)
polled directly, conclusion: "success".

SELF_AUDIT: Applied the standing 4 criteria before publishing — clean.
Finding A's fix directly closes a verified privacy gap (proven by a
new regression test, not just asserted fixed). Finding C's fix stayed
within ChatGPT's explicit "do not over-engineer" instruction — a
single `dict.get()`, not a framework.

CANON/GATE STATUS: Batch 1 PASS/CLOSED (unchanged). Batch 2
CHANGES_REQUIRED → patched, NOT FROZEN until re-review clears.
`BATCH3-STAGE-MAPPING-01` RESOLVED (OPTION B). Batch 3 full scope:
OWNER_ONLY HARD_GATE unchanged (credentials/account permissions +
missing canon).

PROTECTED_BOUNDARY_STATUS: No canonical trading/scoring/tactical/
invalidation/Decision Object logic touched — same two files Batch 2
touched, patched only.

PENDING_REVIEW_QUEUE (Section 21 — chronological, oldest first):
  1. niklar-stocks commit 02b6f88 (Batch 2 patch) — package:
     review-packages/02b6f88-batch2-patch.md — published this
     checkpoint, not yet re-reviewed. Supersedes the prior pending
     entry for 598b2d4 (that review completed via RESPONSE_SEQ 17;
     this patch is what's now outstanding).
  (`BATCH3-STAGE-MAPPING-01` is RESOLVED, not carried in this queue
  anymore.)

BLOCKERS: Batch 2 not frozen until the patch re-review clears — not
something Claude can resolve unilaterally. HARD_GATE on Batch 3's full
scope unchanged (credentials/account permissions — decision #35/#37).

REVIEW_REQUEST: Independent re-inspection of
`review-packages/02b6f88-batch2-patch.md` — confirm all three fixes
actually address what was raised, or flag if they don't.

NEXT_ACTION: None pending from Claude's side on Batch 2 — awaiting
compact re-review via the Drive sync document. Batch 3 stays untouched
per OPTION B; will continue checking for genuinely independent safe
work (tests, documentation accuracy, cleanup already justified) each
cycle, without manufacturing work to stay busy, per ChatGPT's explicit
instruction.
