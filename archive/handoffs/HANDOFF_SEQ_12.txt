NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: LEVEL_2_PENDING (Batch 3 partial-mapping question) + HARD_GATE (Batch 3 full scope, unchanged)
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 12
CHECKPOINT_ID: niklar-handoff-2026-08-09-seq12-batch3-stage-mapping-delegated-decision
IMPLEMENTATION_COMMIT: niklar-stocks main @ fbfef73
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Per the user's direct instruction ("continue autonomously under
the delegated-decision protocol until a true OWNER_ONLY hard gate"),
investigate whether any bounded slice of Batch 3 is implementable
despite the credential blocker, using the new three-level protocol
rather than treating the whole batch as one undifferentiated stop.

MATERIAL_DELTA: Investigated mapping 5 of the orchestrator's 13
canonical stages (`master_database_update`, `publication_validation`,
`opportunity_matrix_rebuild`, `postflight_verification`,
`research_memory_gate`) to existing, already-tested deterministic
functions, to see if a credential-free partial slice could be built.
Found a concrete falsification before acting on it: the most tempting
mapping (`opportunity_matrix_rebuild` → `mobile_contract.py`'s
`build_opportunity_landscape()`) is wrong — that function is
explicitly, deliberately a non-ranking display pass-through (decision
#10: "Ranking is canonical decision output owned by the upstream
Opportunity Matrix, not available in this environment"); treating it
as the ranking-*rebuild* stage would have silently misrepresented
unavailable canonical decision logic as already implemented.
`research_memory_gate` has a similar mismatch (Section 6's freshness/
reuse-gating concept vs. `compute_evidence_status()`'s completeness
classification, which is a different thing). Only 2 of 5 candidate
mappings (`master_database_update`, `publication_validation`) survive
scrutiny with reasonable confidence, and even those assume values
supplied pre-computed by an upstream step this repo doesn't have.
`run_pipeline()` requires all 13 handlers before it executes at all
(fails closed on any missing one), so a partial wiring can't be
demonstrated end-to-end regardless — the only real deliverable would
be standalone functions whose correctness hinges on an interpretive
mapping decision.

Classified this as genuine LEVEL 2 CHATGPT_DELEGATED_DECISION rather
than guessing unilaterally (decision #40). Recorded decision request
`BATCH3-STAGE-MAPPING-01` (full fields below) in the pending-review
queue. No niklar-stocks mutation for Batch 3 this cycle — the
dependent branch stays held pending ChatGPT reconciliation, per the
protocol's own default ("unless the frozen canon makes one option
unambiguously mandatory" — it doesn't here).

Per "Claude may continue independent work that does not depend on
that decision," landed one more safe housekeeping fix while this item
is held: `AGENTS.md`'s `ops/` category summary predated Batch 1/2,
same gap as the prior `ops/README.md` fix (HANDOFF_SEQ 11). Commit
fbfef73, documentation only. Recorded as decision #39 (decision #38
was the prior README fix, already reported at HANDOFF_SEQ 11).

VALIDATION: unittest 148/148, mypy 0/15, PWA smoke PASS — unaffected
by the doc-only change. Real GitHub Actions run 31279067079 (commit
fbfef73) polled directly, conclusion: "success".

SELF_AUDIT: The stage-mapping investigation itself is the self-audit
— found and rejected an unsafe mapping (opportunity_matrix_rebuild)
before acting on it, rather than after. AGENTS.md change is
documentation-accuracy only, LEVEL 1 CLAUDE_DECIDES.

CANON/GATE STATUS: HARD_GATE (Batch 3 full scope, unchanged) +
LEVEL_2_PENDING (the narrower stage-mapping question, newly recorded).
Batch 1 (221020b) and Batch 2 (598b2d4) remain Frozen/PASS.

PROTECTED_BOUNDARY_STATUS: No canonical trading/scoring/tactical/
invalidation/Decision Object logic touched. No niklar-stocks code
change this cycle — documentation only (AGENTS.md).

PENDING_REVIEW_QUEUE (Section 21 — chronological, oldest first):
  1. niklar-stocks commit 598b2d4 (Batch 2) — package:
     review-packages/598b2d4-batch2-normalization-llm-policy.md —
     published HANDOFF_SEQ 9, not yet audited.
  2. LEVEL 2 decision request BATCH3-STAGE-MAPPING-01 — published this
     checkpoint, not yet reconciled. Full fields:
       DECISION_ID: BATCH3-STAGE-MAPPING-01
       QUESTION: Should Claude implement standalone (not
         `run_pipeline`-wired, since that needs all 13) handler
         functions for orchestrator stages that plausibly have
         existing deterministic coverage, and if so, which
         interpretive stage-to-function mapping is correct?
       OPTIONS:
         A. Implement individually-tested standalone handlers only for
            the 2 stages with defensible mappings after scrutiny —
            `master_database_update` (apply_strategic_update/
            apply_tactical_update) and `publication_validation`
            (existing validator) — explicitly declining
            `opportunity_matrix_rebuild`/`research_memory_gate`/
            `postflight_verification` as unverified.
         B. Do nothing on Batch 3 until credentials AND the missing
            Decision-Object/report-serialization canon are available;
            treat the whole batch as atomic since a partial pipeline
            can't run end-to-end regardless.
         C. Build no-op/pass-through stubs for all 13 purely to
            demonstrate the pipeline runs mechanically — rejected by
            Claude: doesn't advance real functionality, risks implying
            more progress than exists.
       CLAUDE_RECOMMENDATION: Option A, narrowed to only the 2
         defensible mappings.
       APPLICABLE_CANON: `ops/README.md`'s no-I/O scope; Batch 1's
         "minimum-sufficient reversible implementation, document it"
         precedent; decision #10's explicit ranking-logic-not-
         available boundary (the concrete falsification above).
       REVERSIBILITY/DOWNSTREAM_IMPACT: Total — new standalone
         functions only, no existing file touched, nothing wired into
         `run_pipeline()`, no behavior change for any current caller.
       DEADLINE/DEPENDENCY: None hard. Blocks further Batch 3 progress
         toward an actual `run_pipeline()` invocation regardless
         (still separately gated on credentials/canon either way).

BLOCKERS: HARD_GATE on Batch 3's full scope (credentials/account
permissions — unchanged, decision #35/#37). LEVEL 2 hold on the
narrower BATCH3-STAGE-MAPPING-01 question (decision #40) — not
owner-only, but held pending ChatGPT reconciliation per the protocol's
default.

REVIEW_REQUEST: (1) Independent audit of the queued Batch 2 package
(PENDING_REVIEW_QUEUE item 1). (2) Reconcile BATCH3-STAGE-MAPPING-01
(item 2) — confirm/correct/choose among Options A/B/C, or provide the
missing specification for the 3 stages scrutiny couldn't confirm.

NEXT_ACTION: Awaiting ChatGPT's reconciliation of
BATCH3-STAGE-MAPPING-01 and/or independent audit of the Batch 2
package. Will implement Option A (or whatever ChatGPT reconciles to)
once resolved. Continuing to check for safe, independent housekeeping/
testing/documentation each cycle per Section 21 + the user's
"continue autonomously" instruction; will report honestly if none is
found rather than manufacture work.
