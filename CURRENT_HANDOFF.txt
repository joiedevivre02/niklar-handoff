NIKLAR — CURRENT HANDOFF
Last updated: 2026-08-07 (autonomous session, continuous — no blockers)
Maintained by: Claude, working in the private niklar-stocks environment

LATEST DELTA (this update)
------------------------------
niklar-stocks commit aae6fea: added the Home / Opportunity Landscape
mobile view (PWA priority #1) — ops/mobile_contract.py gained
to_opportunity_card()/build_opportunity_landscape() (condensed
multi-ticker rows over existing schema fields; deliberately never
sorts/ranks — that's canonical decision output this environment can't
compute). pwa/ restructured into two independent sections (Opportunity
Landscape + the existing Quick Review) so one section's failure can't
break the other. 10 new tests (47/47 suite passing); re-validated in a
real browser (order-preservation and failure-isolation both proven).
Pre-commit gate PASS (10/10), post-commit verification PASS. Full
detail below under "ops/ + PWA history."
No blockers, no drift, nothing awaiting reconciliation beyond the
existing PENDING CANONICAL RECONCILIATION flag on all private-repo
code artifacts (unchanged rationale — see DECISIONS_CURRENT.txt).
Next action: continue with further safe increments (see
TODO_CURRENT.txt) — same architecture-decision item as before remains
the only thing held for the user.

STATUS
------
RESUMED. ChatGPT independently verified CLAUDE_STATUS.txt and the user
relayed approval ("Startup handshake APPROVED... Set SAFE TO CONTINUE:
YES"). CLAUDE_STATUS.txt has been updated to record the acknowledgement.
Autonomous implementation continued under the same hard rules (pre-
commit gate + post-commit verification run and PASSED on every commit
below; drift kill switch did not trigger). The private canonical Niklar
system (Google Drive) and the private niklar-stocks repository remain
the source of truth; nothing below has been promoted into Drive canon.

PROJECT SUMMARY (sanitized)
----------------------------
Niklar is a research and decision-automation system covering PSE
stocks, US stocks, options, and crypto, governed by a canonical
rulebook and multi-market routing/validation contracts in a private
Google Drive folder. The private niklar-stocks repository now holds:
curated canonical reference docs (docs/niklar-reference/), and (new
this session) a first slice of deterministic operational code
(ops/) with a test suite.

CANONICAL FILES THE NEXT AGENT SHOULD CONSULT
-----------------------------------------------
In the PRIVATE niklar-stocks repository under docs/niklar-reference/
(file names only, not reproduced here):
  00_MANDATORY_READ_CURRENT.txt, START_HERE_CURRENT_STATUS.txt,
  README_FIRST_CURRENT.txt, NIKLAR_MULTI_MARKET_AUTHORITY_MAP_CURRENT.txt,
  PSE_EXECUTION_MANIFEST_CURRENT.txt, US_STOCKS_EXECUTION_MANIFEST_CURRENT.txt,
  OPTIONS_EXECUTION_MANIFEST_CURRENT.txt, CRYPTO_EXECUTION_MANIFEST_CURRENT.txt,
  NIKLAR_VALIDATION_ORCHESTRATOR_CURRENT.txt, NIKLAR_RUN_RECEIPT_CONTRACT_CURRENT.txt

In Drive (not yet mirrored anywhere, read directly):
  NIKLAR_CHATGPT_CLAUDE_HANDOFF_RECOVERY_RULE.txt — governs this
  handoff repo's use, ChatGPT/Claude authority order, stale-canon gate,
  and Claude model-routing/token-efficiency guidance. See this repo's
  README.md for the authority-order and stale-canon-gate summary now
  reconciled into it.

Drive remains authority #1 above all of the above; re-sync if stale.

ops/ + PWA history (private niklar-stocks repo) — full detail
------------------------------------------------------------------
Prior turns (already in earlier handoff versions, unchanged):
  - Repository initialized; docs/niklar-reference/ synced (10 docs +
    index); .gitignore added; no LICENSE; reference-docs branch merged
    to main.

THIS INCREMENT — ops/ integration (commit cac4df1 on niklar-stocks main):
  - Pulled three existing Niklar Python modules from the canonical
    Drive folder (Operations/Shared Workflow) into niklar-stocks/ops/,
    VERBATIM, unmodified:
      * niklar_stocks_ops_v4.py — Master Database schema v4.0 lock,
        strategic-vs-dynamic field-ownership enforcement, and
        deterministic preopen/intraday/near-close card renderers.
      * niklar_single_stock_publication_validator.py — fail-closed
        31-section canonical report validator.
      * niklar_single_stock_orchestrator.py — canonical execution
        pipeline stage runner (stage list + fail-closed sequencing;
        no stage-handler implementations).
  - Wrote a 26-test unittest suite (niklar-stocks/tests/) against
    synthetic fixture data (no real tickers/prices/research content):
    schema-lock violations (missing/extra/reordered columns),
    field-ownership boundaries, all three renderers (incl. fail-closed
    behavior on missing fields and "Unknown" null handling), the
    publication validator, and orchestrator stage-ordering/failure
    behavior.
  - Ran the suite against the unmodified source: ALL 26 PASS. No bugs
    found in the existing modules (one apparent schema-hash-length
    discrepancy was checked and is a non-issue — the hash matches).
  - Added GitHub Actions CI (.github/workflows/tests.yml) to run the
    suite on every push/PR.
  - Updated niklar-stocks root README.md to document ops/ and link
    this handoff repo.
  - This is integration + verification of already-existing Niklar
    artifacts, NOT new trading/decision logic. No stage-handler
    implementations, no Drive/Sheets/Slack integration, no market-data
    or broker connectivity were added — deliberately deferred, see
    TODO_CURRENT.txt.

NEXT INCREMENT — mobile contract + PWA vertical slice
  (commit 56dd1d2 on niklar-stocks main, after startup handshake ack):
  - Rationale: no live-data credentials exist in this environment
    (no PSE EDGE/Sheets/Slack access), so the orchestrator's
    stage-handler work stays correctly blocked on the user's
    architecture decision (see TODO_CURRENT.txt). The PWA/mobile
    output, however, is explicit canonical product direction that
    needs no external credentials — a thin client over already-tested
    canonical fields, buildable and testable entirely with synthetic
    data. That made it the next already-authorized, unblocked
    increment.
  - Added a mobile-facing Decision Object contract (pure structural
    transform of existing canonical Master Database fields — no new
    fields invented, no decision-language reconstruction, and
    deliberately does NOT assert LIVE/CURRENT/STALE freshness since no
    real-time feed exists here — see the module's own documentation for
    the "don't fabricate confidence" rationale).
  - Implemented the "Quick Review / Tactical Card" mobile view (the
    canonical PWA priority-#2 view) as a private-access-only,
    dependency-free static web app: installable app shell, service
    worker that caches the shell but never the data (freshness over
    offline availability), synthetic sample data only. Explicitly
    private: no deployment, no public hosting, noindex/private banner
    in the UI.
  - Test coverage: 11 new unit tests (37/37 total suite passing).
    Additionally validated end-to-end in a real headless browser this
    session (used transiently for validation only, not added as a
    project dependency): happy-path rendering, empty-response,
    HTTP-error, retry-recovery, and malformed-response states all
    verified working, not merely assumed. A stdlib-only HTTP smoke
    test is wired into CI for ongoing regression coverage.
  - Explicitly out of scope this slice (by design, not oversight): the
    Home/Opportunity Landscape view, Daily Brief/Inbox, Watchlist, any
    live data connection, any deployment.
  - Pre-commit gate run and PASSED (all 10 points); post-commit diff
    verification run and PASSED (no secrets, no real ticker-looking
    data beyond the synthetic DEMO1/2/3 fixtures, no deploy config).

DECISIONS ALREADY FROZEN
--------------------------
See DECISIONS_CURRENT.txt.

UNRESOLVED / BLOCKERS
------------------------
  - No blockers currently. Session continuing to further safe,
    unblocked increments.
  - Architecture/stack decision for stage-handler implementations
    (Drive/Sheets/Slack/market-data integration) is explicitly NOT
    being made autonomously — see TODO_CURRENT.txt
    "AWAITING USER DECISION". This is exactly the kind of "material
    architectural decision with no existing authoritative basis" the
    operating instructions say to hold for the user rather than guess.

PENDING CANONICAL RECONCILIATION
-----------------------------------
  - The ops/ integration and the mobile contract/PWA slice (both this
    session) exist only in the private niklar-stocks repo. Not
    promoted into Drive canon (code artifacts are not expected to
    live in Drive — flagged per the recovery rule's requirement to
    label not-yet-Drive-reconciled state explicitly, not because
    promotion is expected here).
  - This handoff repo's README.md authority-order/stale-canon-gate
    section was reconciled against
    NIKLAR_CHATGPT_CLAUDE_HANDOFF_RECOVERY_RULE.txt in Drive earlier
    this session (adding the 4th authority tier and the 10-step gate
    procedure).

NEXT RECOMMENDED STEPS
-------------------------
  - Continue with further safe, bounded technical-debt/reliability
    increments per the stated priority order (see TODO_CURRENT.txt) —
    e.g. the next PWA views (Home/Opportunity Landscape, Watchlist)
    once there's real portfolio-level data to back them, or further
    ops/ hardening.
  - Architecture decision for stage-handler implementations (and, by
    extension, a live data source for the PWA) remains with the user.

FILES / COMMITS / BRANCHES (private niklar-stocks repo, for reference)
--------------------------------------------------------------------------
  - Default branch: main
  - Branch: claude/niklar-folder-reference-vzytcg (merged, still on
    origin)
  - Commits on main: 1f7f4d8 (initial) -> b0dd27f (.gitignore) ->
    eb0a196 (reference docs) -> 98cbf5a (merge) ->
    cac4df1 (ops/ integration + tests + CI) ->
    56dd1d2 (mobile contract + PWA vertical slice) ->
    aae6fea (Opportunity Landscape view)
