NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: PWA_PRIVATE_SNAPSHOT_BRIDGE_01_LANDED — STOPPED FOR CHATGPT REVIEW
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 27
CHECKPOINT_ID: niklar-handoff-2026-08-09-seq27-pwa-private-snapshot-bridge-01
IMPLEMENTATION_COMMIT: niklar-stocks main @ 12bb227
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Owner priority (Drive ChatGPT->Claude sync RESPONSE_SEQ 24,
responding to HANDOFF_SEQ 26): make the existing private PWA
consumable with a real canonical Master Database snapshot, targeting
a usable private build "today" — no public endpoint, no live API, no
new scoring logic, no second source of truth.

MATERIAL_DELTA: New file `ops/mobile_snapshot_export.py` — reads a
caller-supplied LOCAL canonical Master Database snapshot (CSV or
JSON), validates it via `niklar_stocks_ops_v4.validate_schema()` (the
SAME schema-lock used elsewhere, not a parallel validator — fails
closed on any extra/missing/reordered column), transforms rows
through `mobile_contract.py`'s own unmodified functions (no scoring/
ranking logic added, input order preserved exactly), and writes a
versioned mobile snapshot to `pwa/private-data/` (`.gitignore`d, never
committed — verified via `git check-ignore` for the directory and
every generated filename).

`pwa/app.js` resolves DEMO vs PRIVATE SNAPSHOT mode once per load. The
ONLY path to DEMO fallback is a clean 404 on `private-data/meta.json`
or total server unreachability; any other outcome commits to PRIVATE
mode and shows an honest per-section error rather than ever silently
substituting demo data. **Verified in a real headless-browser pass**
(Playwright/Chromium, transient — not a project dependency): (1)
PRIVATE mode with real synthetic data — correct banner + real ticker
rendered; (2) DEMO mode, 404 case — correct fallback; (3) the critical
configured-but-broken case — 0 rendered tickers, explicit `HTTP 404`
error, zero silent fallback.

Found and fixed a genuine pre-existing gap: `python3 -m http.server
8000` alone binds ALL network interfaces by default (confirmed
empirically — "Serving HTTP on 0.0.0.0"), not localhost-only. The
documented default now explicitly binds `127.0.0.1`; LAN mode is a
separate, clearly-labeled, opt-in, temporary section with a trusted-
network warning.

`service-worker.js` explicitly never caches `private-data/` paths
(defense in depth beyond `app.js`'s own `cache: "no-store"`).

VALIDATION: unittest 285/285 (243→285, 42 new), mypy 0/19, PWA smoke
PASS, `node --check` clean on both changed JS files. Real GitHub
Actions run `31340958910` (commit `12bb227`) polled directly,
conclusion: `"success"`.

SELF_AUDIT: `mobile_contract.py`/`niklar_stocks_ops_v4.py` (both
Drive-verbatim) confirmed unmodified — zero changes to either file in
this diff. No scoring/ranking/trading logic introduced anywhere.
**Explicit confirmation, no real private data committed**: diff
scanned for secret-shaped strings and ticker-like tokens — only
synthetic `DEMO`/`TEST1-3`/`REALX` markers found; `pwa/private-data/`
absent from the working tree and this commit's file list at commit
time; `.gitignore` coverage re-verified via `git check-ignore` after
the commit landed, not just before.

CANON/GATE STATUS: Unchanged elsewhere — Batch 1 PASS/CLOSED, Batch 2
PASS/CLOSED/FROZEN, `BATCH3-STAGE-MAPPING-01` RESOLVED (OPTION B),
Batch 3 at 7 of 13 real stages, `PIPELINE-CONTINUITY-SCOPE-01`
RESOLVED (OPTION C). NEW: `PWA-PRIVATE-SNAPSHOT-BRIDGE-01` implemented
and pending review.

PROTECTED_BOUNDARY_STATUS: No canonical trading/scoring/tactical/
invalidation/Decision Object logic touched. No public hosting, no
live backend/API — both explicitly authorized-against by this batch's
own scope and confirmed absent.

PENDING_REVIEW_QUEUE (Section 21):
  1. niklar-stocks commit 12bb227 —
     review-packages/12bb227-pwa-private-snapshot-bridge-01.md
     (published, not yet audited; includes the 3-scenario manual
     browser verification and known limitations)

BLOCKERS: Unchanged — the 6 deferred orchestrator stages, the actual
Slack/PWA *live* delivery layer, manual-entry field typing, and any
future live/hosted backend remain genuinely blocked on credentials/
specification/authorization this repo doesn't have. The local private-
snapshot bridge (this batch) does NOT resolve the held live-data
decision — it's a one-shot local export, not a live source. `PRELIVE
GOVERNANCE AUDIT RELEASED: NO` — still deferred, not self-triggered.

REVIEW_REQUEST: Independent review of commit `12bb227` — exact SHA,
files changed, test delta, known limitations, and explicit no-real-
data-committed confirmation are all in the review package per the
batch's own requirement.

NEXT_ACTION: Per the batch's explicit "then stop for ChatGPT review
before any broader next increment" instruction: **stopped here.** No
further implementation increment starts until ChatGPT reviews this
checkpoint (or the owner gives new direction). Governance-compliance
audit remains deferred, not self-triggered.
