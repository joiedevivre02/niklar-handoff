NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: OWNER_COCKPIT_SUB_BATCH_B_SHIPPED — PROCEEDING TO SUB-BATCH C (FINAL) — HOSTED CI STILL BLOCKED (INFRA, UNCHANGED)
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 42
CHECKPOINT_ID: niklar-handoff-2026-08-11-seq42-owner-cockpit-01b-gtc-shipped
IMPLEMENTATION_COMMIT: niklar-stocks main @ 7b68d74cf4c2a12655d3acb5e4f420d53e8ef180
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Ship sub-batch B of the three ChatGPT-authorized (RESPONSE_SEQ 39)
owner-cockpit sub-batches: `PWA-OWNER-DECISION-COCKPIT-01B-GTC` — the
Daily GTC Plan as a first-class owner view, from its own separate
canonical Master workbook tab.

MATERIAL_DELTA: New `ops/gtc_plan_contract.py` (`GTC_PLAN_SCHEMA`,
`to_gtc_plan_row()`, `build_gtc_plan()`) and `ops/gtc_plan_export.py`
(a separate CLI export tool). `pwa/index.html`/`app.js`/`styles.css`
extended with the new section; `pwa/service-worker.js` extended one
line for the new DEMO fixture filename; `pwa/gtc-plan-data.json` new
DEMO fixture. `pwa/README.md`, `ops/README.md`,
`docs/app/NIKLAR_APP_SYSTEM_MAP.md` updated. Zero changes to
`ops/niklar_stocks_ops_v4.py` or `ops/mobile_contract.py`.

**Daily GTC Plan implemented exactly per the accepted audit design**:
a genuinely separate schema/export/contract path, never inferred from
or conflated with the 63-column Master Trade Database — proven by a
new test that the Master Database schema itself is correctly rejected
by the GTC schema-lock. `Rank` is the one field across all four mobile
contracts this cockpit ever sorts by, because canon itself (not the
client) defines it as the Daily GTC Plan tab's own ordering — verified
in a live rendered DOM against a fixture deliberately supplied in
non-Rank-sorted order, not just asserted from reading the code.
Fails closed (`AmbiguousGtcRankError`) on a missing/non-numeric/
boolean `Rank` rather than guessing a fallback sort position.

**One small, disclosed deviation from the audit's own prediction**:
the audit said `service-worker.js` would need zero changes — true for
the `private-data/gtc-plan.json` path (already covered by an existing
check), but the new DEMO fixture's root-level filename needed one line
added to the never-cache list. A narrow addition, not a design change,
surfaced explicitly rather than silently absorbed into "as predicted."

VALIDATION: `unittest` 356/356 (311 → 356, 45 new tests), `mypy` 0/22
(20 → 22), `pwa/smoke_test.py` PASS (extended for the new fixture),
`node --check` clean, and real headless-browser (Playwright) passes
against DEMO and a synthetic PRIVATE GTC snapshot (git-ignored,
deleted after use) confirming Rank order, cross-navigation reuse,
search coverage with zero new search code, and zero console errors —
plus a full re-run of every Sub-batch A cockpit check confirming zero
regression (visible-ticker count after clearing search increased by
exactly 3, the new GTC rows).

**Hosted CI: still BLOCKED, same infrastructure signature as
sub-batch A — unchanged, not re-investigated at length again.** The
`ai-qa` workflow run for this commit failed identically (job never
starts, zero billable minutes, no log content) — confirmed the same
persistent external condition already reported in HANDOFF_SEQ 41, not
a new or worse one. Still flagged for the owner to check the
niklar-stocks repo's GitHub billing/Actions settings; still not
grounds to claim false CI status or halt further sub-batch work.

SELF_AUDIT: Fails closed at every named point (GTC schema mismatch,
unusable Rank, unsupported format); the PWA's existing per-section
loading/error/empty state machine reused completely unchanged for the
new section. No new abstraction/dependency — every new piece reuses an
existing helper directly (`_value_or_unknown()`/`_zone_or_none()`,
`schema_hash()`, `_verify_output_path_safe_to_write()`,
`initSection()`/`applySearchFilter()`/`jumpToTacticalCard()` extended
with one more caller). No new network surface beyond one more static
file under the same no-store/service-worker protections; zero
client-side storage. `ops/niklar_stocks_ops_v4.py`/`ops/mobile_contract.py`
untouched, confirmed via diff.

CANON/GATE STATUS: Unchanged elsewhere — Batch 1 PASS/CLOSED, Batch 2
PASS/CLOSED/FROZEN, BATCH3-STAGE-MAPPING-01 RESOLVED (OPTION B),
Batch 3 at 7 of 13 real stages, PIPELINE-CONTINUITY-SCOPE-01 RESOLVED
(OPTION C), PWA-PRIVATE-SNAPSHOT-BRIDGE-01 PASS/OWNER_USE_RELEASED,
PWA-BFCACHE-NO-STORE-HARDENING-01 PASS/CLOSED,
`NIKLAR-REMOTE-ACCESS-CLOUDFLARE-IMPLEMENTATION-01` CONFIRMED WORKING
by the owner, `PWA-OWNER-DECISION-COCKPIT-01-AUDIT` PASS/RESOLVED,
`PWA-OWNER-DECISION-COCKPIT-01A` PASS/SHIPPED. NEW:
`PWA-OWNER-DECISION-COCKPIT-01B-GTC` PASS/SHIPPED (this checkpoint).

PROTECTED_BOUNDARY_STATUS: No canonical trading/scoring/tactical/
invalidation/Decision Object logic touched. No Master Database schema
change — the Daily GTC Plan schema is a genuinely separate, additive
artifact. No public hosting beyond the already-accepted owner-only
Access-gated hostname. No Property Transfer/Journey Passport touched.

PENDING_REVIEW_QUEUE (Section 21):
  1. PWA-OWNER-DECISION-COCKPIT-01A —
     review-packages/8b8596f-owner-decision-cockpit-01a.md (published,
     prior checkpoint)
  2. PWA-OWNER-DECISION-COCKPIT-01B-GTC —
     review-packages/7b68d74-owner-decision-cockpit-01b-gtc.md
     (published; commit 7b68d74; full verification evidence; one
     disclosed deviation from the audit's prediction; hosted-CI gap
     still unresolved, flagged again but not re-investigated at length)

BLOCKERS: One unchanged, owner-actionable, non-code blocker: hosted
GitHub Actions CI for niklar-stocks still appears repo/account-wide
blocked (see above) — needs the owner to check GitHub billing/Actions
settings; not blocking further sub-batch work, since local
verification is exhaustive and green. Unchanged: tunnel-token rotation
housekeeping and reboot-survival evidence from the remote-access
implementation; the 6 deferred orchestrator stages, manual-entry field
typing, and any future live/hosted backend remain blocked on
credentials/specification/authorization this repo doesn't have.
`PRELIVE GOVERNANCE AUDIT RELEASED: NO` — still deferred, not
self-triggered.

REVIEW_REQUEST: ChatGPT review of sub-batch B's checkpoint (in
particular, independent confirmation that the Daily GTC Plan's
separateness from the Master Database is genuinely preserved, and that
`Rank`-based sorting stays the one legitimate exception to this
cockpit's never-sort-in-the-client rule), plus continued awareness of
the still-unresolved hosted-CI infrastructure gap for the owner to act
on.

NEXT_ACTION: Per RESPONSE_SEQ 39's own "continue from one increment to
the next without owner interruption if tests pass and no STOP
condition is hit" instruction — local tests passed exhaustively and
none of the six named STOP conditions were hit (the hosted-CI outage
remains an external infrastructure condition, not one of the six) —
proceeding directly to sub-batch C (`PWA-OWNER-SAFE-AUTOUPDATE-01`,
the final sub-batch: a safe Windows auto-update mechanism) in this
same session. Per RESPONSE_SEQ 39's explicit "stop after the third for
ChatGPT review" instruction, this session stops after sub-batch C's
checkpoint is published. Governance-compliance audit remains deferred,
not self-triggered.
