NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: OWNER_AUTHORIZED_BATCH3_CONTINUATION (increment 2 landed, async review requested, 2 packages pending)
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 18
CHECKPOINT_ID: niklar-handoff-2026-08-09-seq18-batch3-increment2-evidence-acquisition
IMPLEMENTATION_COMMIT: niklar-stocks main @ aca2a86
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Continue Batch 3 implementation per the owner's explicit
"handoffs given as you work, ChatGPT reviews later" instruction
(decision #47) — increment 2, without waiting for increment 1's
review to land first (Section 21's async-review model).

MATERIAL_DELTA: Added `make_evidence_acquisition_handler()` to
`ops/niklar_orchestrator_stage_handlers.py` — the **7th of 13**
canonical stages to get a real, grounded handler (was 6 after
increment 1). Acquires one evidence field via a caller-injected fetch
adapter (the actual Drive/news/market-data call this environment has
no credentials for — honestly deferred, not fabricated), then
normalizes the fetched value through Batch 2's already-tested,
already-independently-reviewed `attempt_normalization()` (commit
02b6f88). Grounded in architecture doc section 1 ("Research evidence
must be normalized before consumption") and the absence of any
separate named "normalization" stage among the 13 — this stage owns
both acquire and normalize; only the acquisition mechanism is
genuinely unavailable here.

Two failure modes handled per the architecture doc's own distinction:
a fetch-adapter failure propagates and stops the pipeline (a genuine
stage failure, matching `run_pipeline()`'s existing fail-closed
design); a normalization failure degrades non-blockingly (returns
successfully with a "degraded" detail) per architecture doc section 3
— mirroring `attempt_normalization()`'s own non-blocking contract
rather than overriding it with `run_pipeline()`'s stricter default.

VALIDATION: unittest 178/178 (175 → 178, 3 new: successful fetch+
normalize, non-blocking degradation on a normalization failure,
propagation on a fetch failure), mypy 0/16 (unchanged file count), PWA
smoke PASS. Real GitHub Actions run 31297477786 (commit aca2a86)
polled directly, conclusion: "success".

SELF_AUDIT: Applied the standing 4 criteria — clean. Two distinct
failure modes tested explicitly (propagate vs. degrade), not picked
uniformly. No new dependency/file. No I/O/credentials/secrets. The 3
Drive-verbatim `ops/` modules remain untouched (confirmed via `git
diff`).

CANON/GATE STATUS: Batch 1 PASS/CLOSED. Batch 2 PASS/CLOSED/FROZEN.
`BATCH3-STAGE-MAPPING-01` RESOLVED (OPTION B, unchanged). Batch 3
full-scope HARD_GATE remains downgraded (by direct owner authorization,
decision #47) to "implement what's genuinely groundable; defer what
genuinely isn't." 7 of 13 stages now real; 6 remain genuinely deferred
(`canonical_serialization`, `research_database_append`,
`research_indexes_update`, `opportunity_matrix_rebuild`,
`discovery_consumers_sync`, `tactical_consumers_sync`).

PROTECTED_BOUNDARY_STATUS: No canonical trading/scoring/tactical/
invalidation/Decision Object logic touched or fabricated. The 3
Drive-verbatim `ops/` modules remain untouched.

PENDING_REVIEW_QUEUE (Section 21 — chronological, oldest first):
  1. niklar-stocks commit 521f5a9 (Batch 3 increment 1) — package:
     review-packages/521f5a9-batch3-increment1-stage-handlers.md —
     published HANDOFF_SEQ 17, not yet audited.
  2. niklar-stocks commit aca2a86 (Batch 3 increment 2) — package:
     review-packages/aca2a86-batch3-increment2-evidence-acquisition.md
     — published this checkpoint, not yet audited.

BLOCKERS: None new. The 6 remaining deferred stages stay genuinely
blocked on credentials/proprietary canon — not something Claude can
resolve unilaterally regardless of authorization to proceed on the
groundable subset.

REVIEW_REQUEST: Independent async review of both queued packages —
confirm or correct the stage mappings (2 MEDIUM-confidence from
increment 1: `research_memory_gate`, `postflight_verification`; the
new `evidence_acquisition` mapping from increment 2), and confirm the
6 still-deferred stages remain correctly left for injected adapters.

NEXT_ACTION: Continuing to look for further groundable increments
without waiting for review to land first, per the owner's explicit
instruction — will incorporate any correction ChatGPT surfaces on the
next `sync`. Will report honestly if no further genuinely groundable
increment exists among the remaining 6 deferred stages, rather than
force one.
