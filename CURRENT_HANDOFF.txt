NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: PASS
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 6
CHECKPOINT_ID: niklar-handoff-2026-08-09-seq6-prelive-batch1-state-contracts
IMPLEMENTATION_COMMIT: niklar-stocks main @ 53d2b3f
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Implement pre-live architecture Batch 1 only (contracts/state
layer), per the owner-approved architecture (Drive
`NIKLAR_PRELIVE_ARCHITECTURE_DECISIONS_CURRENT`, RESPONSE_SEQ 8,
IMPLEMENTATION AUTHORIZATION: RELEASED) and the explicit staged-batch
discipline ("Do not attempt one giant refactor... begin Batch 1
only"). Batches 2-10 are not started.

MATERIAL_DELTA: New module `ops/niklar_state_contracts.py` — pure,
deterministic data shapes and pure builder/classification functions,
no I/O/credentials/connectivity, matching `ops/`'s existing
discipline. Implements exactly the 6 pieces the architecture doc names
for Batch 1: `compute_evidence_status()` (doc §3, EVIDENCE_COMPLETE/
CONDITIONAL/INSUFFICIENT classification), `new_intraday_observation()`
(§7, the one contract every intraday adapter must emit, fail-closed on
an unrecognized source), `new_change_ledger_entry()`/`append_change()`
(§14, append-only), `new_review_queue_entry()`/`append_review_entry()`
(§14's 8 named trigger conditions, fail-closed on an unrecognized
reason), `new_llm_review_request()` (§§2,4 — always starts PENDING, no
kwarg exists to bypass that), `build_current_state_snapshot()` (§14 —
pure aggregation, computes its own counts so they can't drift from
inputs). `market_state` (§14's rankings/watchlist/positions field) is
a generic optional passthrough marked INTERFACE GAP, not a guessed
sub-schema — same pattern as `NIKLAR_APP_CONTRACT.md`'s existing
real-vs-simulated-position gap. Full diff published in
`review-packages/53d2b3f-prelive-batch1-state-contracts.md` (not
duplicated here) since ChatGPT's connector can't reach the private
repo directly.

VALIDATION: unittest 122/122 (95->122, 27 new), mypy 0/14 files, PWA
smoke unaffected. Real GitHub Actions run 31276045591 polled directly,
conclusion: "success".

SELF_AUDIT: Applied the standing 4 criteria (failure envelope,
unnecessary abstraction/dependency, security/privacy, canonical
boundary drift) before publishing — clean, no findings. Detail in the
review package.

CANON/GATE STATUS: PASS; EXCEPTIONS: NONE.

PROTECTED_BOUNDARY_STATUS: No canonical trading, scoring, tactical,
invalidation, or Decision Object *logic* implemented — this batch is
pure state/audit scaffolding (status classification, ledgers, a
snapshot builder), not decision logic. No public deployment/hosting/
secret exposure.

BLOCKERS: None.

REVIEW_REQUEST: Independent inspection of
`review-packages/53d2b3f-prelive-batch1-state-contracts.md` (full
diff + self-audit checklist) — first batch of a 10-batch effort, so
review before Batch 2 builds on top of this shape is worth it even
though the REVIEW/HANDOFF rule doesn't strictly require review on
every batch.

NEXT_ACTION: Begin Batch 2 (non-blocking normalization exception path
+ owner-gated LLM policy + deterministic LLM dependency audit tooling)
once review of this batch is either PASS or addressed. Will not start
Batch 2 work before checking for a response, per "self-audit each
batch, don't generate repetitive checkpoints" — next sync will confirm
whether to proceed or address feedback first.
