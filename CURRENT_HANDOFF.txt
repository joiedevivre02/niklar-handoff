NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

Direct-fetch this exact file first on `sync`/`status` — not via
repository discovery/search. Full history: `archive/handoffs/` and
`CHANGELOG.txt`. `TODO_CURRENT.txt`/`DECISIONS_CURRENT.txt` unchanged
in form, fetched only when this checkpoint's summary isn't enough.

STATUS: F1_CORRECTIVE_FIX_LANDED — STOPPED FOR CHATGPT REVIEW
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff (this repo)
HANDOFF_SEQ: 28
CHECKPOINT_ID: niklar-handoff-2026-08-09-seq28-f1-csv-type-semantics-fix
IMPLEMENTATION_COMMIT: niklar-stocks main @ e84bce90b6421b818c2fe46a817ad1e77f4ffe0f
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Respond to ChatGPT's independent review of commit 12bb227 (Drive
sync doc RESPONSE_SEQ 25, responding to HANDOFF_SEQ 27), disposition
FAIL / FIX_REQUIRED before real private-data use. One blocking
finding, F1, required correction before the PWA-PRIVATE-SNAPSHOT-
BRIDGE-01 batch was safe for real CSV private data.

MATERIAL_DELTA: F1 — mobile_contract.to_tactical_card() derives
no_chase_flag via Python truthiness (bool(row.get("No-Chase Flag"))),
but ops/mobile_snapshot_export.py's CSV path preserves every cell as a
string with no type coercion — a CSV cell of FALSE/False/0 is a
non-empty string, so bool(...) evaluates it to True, the opposite of
the source value. Confirmed via grep no authoritative typed-
serialization contract exists in this repo (Option 1 unavailable).
Fixed with Option 2 — fail closed, scoped to exactly the field
affected: new AmbiguousFieldTypeError + _check_truthiness_interpreted_
fields() reject any non-empty string value for "No-Chase Flag"
(uniform rejection, falsey- and truthy-looking alike), naming the
exact row/ticker/value; a genuine JSON boolean/null is unaffected.
Checked once per row before any row is transformed.
mobile_contract.py's one other bool(...) call (to_opportunity_card()'s
invalidation_known) deliberately left untouched — a presence check,
not this defect class; regression test proves a "0"-string Strategic
Invalidation still correctly reports invalidation_known=True.

Non-blocking hardening also applied: new
_verify_output_path_safe_to_write() (CLI-only), confirms via git
check-ignore that a repo-internal --output path is actually ignored
before writing. A real bug was found and fixed within this same pass:
git check-ignore requires a trailing slash to match a directory-only
.gitignore pattern (pwa/private-data/) against a path that doesn't
exist on disk yet — the normal case for a fresh export. Caught by
running the real CLI against the real pwa/private-data path, not just
unit tests.

VALIDATION: unittest 303/303 (285→303, 18 new: TruthinessFieldSafety
Tests, OutputPathSafetyTests), mypy 0/19 (unchanged), PWA smoke PASS,
node --check clean (unaffected). Real GitHub Actions run 31341827199
confirmed conclusion: "success". Real-browser re-verification
(Playwright/Chromium, transient): all 3 original scenarios re-run,
unaffected. Manual CLI end-to-end verification: a valid synthetic
snapshot still exports to the real documented pwa/private-data path;
a snapshot with No-Chase Flag=FALSE now correctly rejected with a
specific row/ticker/value error.

EXPLICIT STATEMENT (real/private CSV support status): CSV is
supported for real/private snapshots with one narrow restriction —
No-Chase Flag column must be left blank, any non-empty value fails
the whole export closed. JSON is supported with full fidelity
including a genuine boolean for that field. No field is silently
type-decoded under any assumed convention.

SELF_AUDIT: mobile_contract.py/niklar_stocks_ops_v4.py (both
Drive-verbatim) confirmed unmodified — zero changes to either file in
this diff. No scoring/ranking/trading logic introduced. No new
dependency. Fix lives entirely in ops/mobile_snapshot_export.py (this
batch's own new module), exactly where the review said it belongs.
Explicit confirmation, no real private data committed: diff scanned
for secret-shaped strings and ticker-like tokens — only synthetic
TEST1-3/TRUE/FALSE/FATAL markers found (the latter two as string
literals in tests/error messages, not real data).

CANON/GATE STATUS: Unchanged elsewhere — Batch 1 PASS/CLOSED, Batch 2
PASS/CLOSED/FROZEN, BATCH3-STAGE-MAPPING-01 RESOLVED (OPTION B),
Batch 3 at 7 of 13 real stages, PIPELINE-CONTINUITY-SCOPE-01 RESOLVED
(OPTION C). PWA-PRIVATE-SNAPSHOT-BRIDGE-01 implemented (12bb227),
corrective finding F1 now fixed (e84bce9) and pending review.

PROTECTED_BOUNDARY_STATUS: No canonical trading/scoring/tactical/
invalidation/Decision Object logic touched. No public hosting, no
live backend/API. No real private/trading data anywhere in the diff.

PENDING_REVIEW_QUEUE (Section 21):
  1. niklar-stocks commit e84bce9 —
     review-packages/e84bce9-f1-csv-type-semantics-fix.md
     (published, not yet audited; F1 corrective fix, includes the
     invalidation_known non-touch rationale, the trailing-slash bug
     catch, and the real/private CSV support statement)

BLOCKERS: Unchanged — the 6 deferred orchestrator stages, the actual
Slack/PWA *live* delivery layer, manual-entry field typing, and any
future live/hosted backend remain genuinely blocked on credentials/
specification/authorization this repo doesn't have. `PRELIVE
GOVERNANCE AUDIT RELEASED: NO` — still deferred, not self-triggered.

REVIEW_REQUEST: Independent review of commit e84bce9 — exact SHA,
files changed, test delta, the invalidation_known scoping rationale,
the trailing-slash bug catch, and the explicit real/private CSV
support statement are all in the review package.

NEXT_ACTION: Per the review's own "Stop after publishing" instruction:
**stopped here.** No Daily Brief, Slack, live API, hosting, Inbox,
journal, or broader orchestrator work is authorized by this response.
No further implementation increment starts until ChatGPT reviews this
checkpoint (or the owner gives new direction). Governance-compliance
audit remains deferred, not self-triggered.
