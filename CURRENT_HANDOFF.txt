NIKLAR — CURRENT HANDOFF (compact latest-pointer form)

STATUS: WINDOWS-NATIVE PSE EDGE SCHEDULER IMPLEMENTED, PUSHED, AND FCG TASK REGISTERED
PROJECT: niklar-stocks (private implementation repo) + niklar-handoff
HANDOFF_SEQ: 47
CHECKPOINT_ID: niklar-handoff-2026-08-12-seq47-pse-edge-desktop-scheduler
IMPLEMENTATION_COMMIT: niklar-stocks repair/pse-eod-freshness-interlock-20260812 @ c3caf88 (parent repair 469a597)
HANDOFF_COMMIT: niklar-handoff main HEAD (this commit)

GOAL: Provide a long-term Windows-desktop PSE EDGE acquisition mechanism that runs without Codex being open.

MATERIAL_DELTA:
- Added ops/windows/Invoke-PseEdgeAcquisition.ps1.
  - Runs the repaired Python PSE EDGE worker through local Edge.
  - Accepts company ID, optional expected session, browser channel, Python path, and log directory.
  - Defaults expected session to the current Asia/Manila date.
  - Resolves the bundled Codex Python runtime when present, otherwise system Python.
  - Writes a timestamped structured JSON envelope under %LOCALAPPDATA%\Niklar\PseEdge\logs.
  - Propagates the worker's fail-closed status; no database or fallback mutation.
- Added ops/windows/Register-PseEdgeAcquisitionTask.ps1.
  - Registers one current-user Scheduled Task per PSE company ID.
  - Weekdays at 6:30 PM, StartWhenAvailable, WakeToRun, IgnoreNew overlap policy, 10-minute limit.
  - Interactive user logon, Limited run level; no stored credential or SYSTEM context.
- Added operator documentation and ops README integration.
- Registered and read back live task NiklarPseEdge-689 for FCG under user bamis.
  - State: Ready.
  - Next run: 2026-08-12 18:30 Asia/Manila.
  - Command targets the checked-in runner at D:\Niklar\niklar-stocks.
- Executed the wrapper once and verified a structured failure log was written; no silent success.

VALIDATION:
- Both PowerShell scripts parse cleanly.
- Registration -WhatIf succeeded before mutation.
- Task registration and readback succeeded.
- Relevant PSE suite: 20 tests plus 5 subtests PASS.
- mypy acquisition module PASS.
- pre/post-commit diff checks PASS.
- Commit c3caf88 pushed to existing repair branch only.

FAILURE ENVELOPE:
- Holidays/non-session weekdays fail closed because expected session defaults to Manila today and must match rendered As Of.
- Missing/incomplete PSE fields remain nonzero failures and are logged.
- If the machine is off, StartWhenAvailable permits a delayed run, but session equality can cause a safe stale failure rather than relabeling data.
- The task currently covers FCG/company ID 689 only. The scripts are generic; one task can be registered per additional maintained company ID.
- The runner logs evidence only. It does not persist into canonical Niklar databases or perform fallback acquisition.

PROTECTED BOUNDARY STATUS: PASS — no fallback, canonical persistence, Decision Object, GTC, Shadow, serializer, PWA, or publication work.

BLOCKERS:
- Live post-rollover Volume/Average remain absent from observed PSE EDGE surfaces.
- Additional ticker task registration requires the authoritative maintained ticker-to-company-ID map; IDs were not guessed.
- Scheduled task uses the currently checked-out repair-branch path. Merge/deployment governance remains pending review.

REVIEW_REQUEST:
Review commits 469a597 and c3caf88. Advise whether to merge the fail-closed repair/scheduler and provide the authoritative company-ID roster if the desktop task should cover more tickers.

NEXT_ACTION: Let the registered FCG task run at 6:30 PM and inspect its local JSON result; do not perform prohibited fallback/downstream work.

ENGINEERING AUTHORITY CHECK: PASS
ENGINEERING SCOPE FIREWALL: PASS
PROPERTY-TRANSFER MATERIAL IN NIKLAR-STOCKS: NO
Compliance: PASS
